#example 3.25 playing with the dicts

params = {"server":"mpilgrims", "database":"master", "uid":"sa", "pwd":"secret"}

print "params.keys()", params.keys()

print "params.values()", params.values()

print "params.items()", params.items()

